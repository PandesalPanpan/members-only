<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Members Only</title>
    <link rel="stylesheet" href="style.css">
</head>

<body>
    <div class="container">
        <%- include("partials/navigation.ejs") %>

        <section>
            <h1 class="muted">Messages</h1>

            <% if (messages && messages.length) { %>
                <div class="messages" role="list">
                    <% messages.forEach(msg => { %>
                        <article class="card" role="listitem" aria-label="Message by <%= msg.username %>">
                            <h3><%= msg.title %></h3>
                            <p class="muted"><%= msg.message %></p>

                            <div class="message-meta">
                              <div class="message-author">— <%= msg.username %></div>
                              <div class="message-added"><%= msg.added ? (new Date(msg.added)).toLocaleString() : '' %></div>
                            </div>

                            <div class="align-right" style="margin-top:.5rem">
                                <% if (locals.isAdmin) { %>
                                    <a class="btn secondary" href="/message/<%= msg.id %>/delete">Delete</a>
                                <% } %>
                            </div>
                        </article>
                    <% }) %>
                </div>
            <% } else { %>
                <p class="muted">No messages yet.</p>
            <% } %>
        </section>
    </div>
</body>

</html>