<nav class="container">
  <a href="/">Home</a>
  <div class="spacer"></div>

  <% if (!isAuthenticated) { %>
    <a class="btn" href="/sign-up">Sign up</a>
    <a class="btn secondary" href="/login">Login</a>
  <% } else { %>
    <a class="btn secondary" href="/create-message">Create</a>
    <a class="btn secondary" href="/membership">Membership</a>
    <a class="btn danger" href="/logout">Logout</a>
    <div class="muted" style="margin-left:.5rem">Welcome, <%= currentUser ? currentUser.username : 'Guest' %></div>
  <% } %>
</nav>